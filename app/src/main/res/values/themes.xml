<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Notes" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_green</item>
        <item name="colorPrimaryVariant">@color/mid_low_green</item>
        <item name="colorOnPrimary">@color/forest_green</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/light_green</item>
        <item name="android:windowTranslucentStatus">true</item>

    </style>


    <!-- Customize your theme here. -->

    <style name="Theme.Notes" parent="Base.Theme.Notes" />


    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/mid_low_green</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/note</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Notes</item>


    </style>

    <style name="Theme.Notes.AppWidgetContainerParent" parent="@android:style/Theme.DeviceDefault">
        <!-- Radius of the outer bound of widgets to make the rounded corners -->
        <item name="appWidgetRadius">16dp</item>
        <!--
        Radius of the inner view's bound of widgets to make the rounded corners.
        It needs to be 8dp or less than the value of appWidgetRadius
        -->
        <item name="appWidgetInnerRadius">8dp</item>
    </style>

    <style name="Theme.Notes.AppWidgetContainer" parent="Theme.Notes.AppWidgetContainerParent">
        <!-- Apply padding to avoid the content of the widget colliding with the rounded corners -->
        <item name="appWidgetPadding">16dp</item>
    </style>


</resources>